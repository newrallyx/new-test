{
  "name": "roadtrip-retrospective-tool",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "build": "tsc -b && vite build",
    "build:icon": "node scripts/create-car-icon.mjs",
    "package:win": "npm run build && npm run build:icon && electron-builder --win portable --x64",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "dev:frontend": "vite",
    "dev:backend": "node backend/src/index.js",
    "start:backend": "node backend/src/index.js",
    "desktop": "npm run build && electron ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "leaflet": "^1.9.4",
    "react-leaflet": "^4.2.1",
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "concurrently": "^9.1.2",
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.local.roadtrip-retrospective-tool",
    "productName": "旅行轨迹记录与规划工具",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "backend/src/**/*",
      "dist/**/*",
      "electron/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "asar": true,
    "win": {
      "icon": "build/icon.ico",
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "portable": {
      "artifactName": "${productName} ${version}.${ext}"
    }
  },
  "description": "Desktop app for roadtrip route recording and planning.",
  "author": "Local User"
}
